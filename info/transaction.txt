records every transactions whenever stock is arrived or updated

add product quantity
-to product talbe -quantity
- to batch table

when saving, make sure the quantity is set to 0 
     -if not then we don't include in saving

create records table 
    -connects row id to new table; // don't need batch number order_product id can fetch it; 
    -id,order_product_id, qty receive, date receive


if data ! empty then the modal will appear that shows delivery history columns and related data

