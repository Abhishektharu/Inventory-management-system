create database inventory;
create table users(
    id int primary key;
    first_name varchar(50) ;
    last_name varchar(40);
    email varchar(50);
    password varchar(100);
    created_at datetime;
    updated_at datetime;
)

insert into users(first_name, last_name, email, password)
values(
    "user", "last", "user@email.com", "12345"
)

MERN /A

M = MONGODB
E = EXpress
R = ReactJS
A = Angular
N = nodeJs

programmingwithmos
c#